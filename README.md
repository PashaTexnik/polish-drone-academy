# Drone Academy Poland

Static React landing page based on the provided design archive.

## Local preview

Serve the folder with any static server and open the local URL. The app does not require a build step.

## Netlify Hosting

The project includes `netlify.toml` and is configured to publish the project root as a static site.

Live site: https://polish-drone-academy.netlify.app
